/**
 * Returns the file path for the icon SVG corresponding to the given type.
 * @param type - The type to get the icon for
 * @returns The file path to the icon SVG
 */
export const getTypeIconSrc = (type: string) => `assets/images/types-icons/${type}.svg`;